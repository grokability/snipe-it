<?php

namespace App\Models\Labels;

final class CustomLabelFonts
{
    /** Fonts the customizer exposes — the only names allowed to reach TCPDF. */
    public const ALLOWED = ['freemono', 'freesans', 'dejavusans'];
}